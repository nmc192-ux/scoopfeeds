# Phase B — Go-Live Runbook
### Event-Graph Integrity + Comprehension Homepage

**Status:** pre-deployment. Full stack committed to `main`, all flags OFF (prod-neutral). Prod (Hostinger) untouched.

---

## Governing principle

All validation to date was **from-empty replay** — the matcher building a clean graph from scratch with the entity system enabled. **Prod is not empty.** Its durable graph was built by the old matcher and still contains the over-merge blob. So the spine of this runbook is:

> Validate the *migration* of the existing prod graph on a **fresh snapshot** before enabling anything on prod, then enable in a fixed order, with a full DB backup as the restore-point before the first graph-mutating step.

---

## The committed stack (all flags default OFF)

| Layer | Commit | Flag (default) |
|---|---|---|
| Entity extraction (ingest) | `6fb4eb2`, `0259dcf` | `ENTITY_EXTRACTION_ENABLED` (OFF) |
| Windowed entity IDF | `a78e89c` | `ENTITY_IDF_ENABLED` (OFF) |
| Entity-overlap gate (matcher) | `63e1adf` | `EVENT_ENTITY_MIN` (0 = OFF) |
| Entity-aware breaker (curative, scheduler-wired + detach) | `5cf0706`, `694d12f` | `EVENT_BREAKER_ENABLED`, `EVENT_BREAKER_DETACH` (OFF); `EVENT_BREAKER_MAX_PASSES`=6; `EVENT_MATCH_TAU`=0.78 |
| Display-layer grouping (render) | `9ee2c3a` | `HOMEPAGE_GROUPING` (OFF) |
| Migrations (additive empty tables) | 008 / 009 / 010 | — |

Comprehension homepage redesign: `c9862ef` (currently reverted via `a614599`; re-appliable). Consumes `related[]` from grouped `/api/events`.

> **Flag-OFF = prod-neutral.** With every flag off, the deployed code is behaviorally identical to today and the migrations only add empty tables. Nothing changes until flags are flipped, in order.

---

## Phases

### Phase 0 — Bank the code ✓ (done)
Full stack on `main`, flags OFF. The 3b-2 merge trigger is reverted/discarded (proven dead end).

### Phase 1 — Fresh-snapshot migration validation *(the gate everything hinges on)*
1. **Re-pull a current prod snapshot.** On Hostinger, take a `.backup` of the live `news.db`, copy it down, and replace the (weeks-stale) `backend/data/prod-snapshot.db` (gitignored).
2. On a **COW copy** of this snapshot, run the full enable sequence (extraction backfill → IDF → gate → breaker → grouping) via the replay/validation harness.
3. **Confirm:**
   - (a) the existing blob event **cleans up** — the breaker splits it, the gate prevents re-merge;
   - (b) the rest of the live graph **survives** — coherent events stay coherent, dossier ids stable, no mass churn;
   - (c) display-grouping yields the **shippable homepage** on this real graph.
4. **Possible spawn:** if the blob does *not* self-clean (the breaker fires on re-clustering; a fully-blended durable blob may sit stuck), build a one-time **re-cluster migration script** and re-validate.

- **No prod risk** (COW; read-only on prod).
- **GATE:** blob dissolved, coherent events + dossier ids preserved, homepage shippable. *Do not proceed until green.*

### Phase 2 — Wire the breaker into the scheduler
- The breaker (`5cf0706`) is committed but only ever run via the harness — it is **not** called in the live cycle. Add the post-promotion call in the scheduler/promoter cycle behind `EVENT_BREAKER_ENABLED`. Commit (flag OFF).
- Validate the wired path on COW reproduces the harness-tested behavior.
- **GATE:** flag OFF → scheduler unchanged; flag ON (COW) → breaker runs as validated.

### Phase 3 — Deploy code to Hostinger (flags OFF) — DONE
**Prod run model (actual):** Hostinger **LiteSpeed Node** — a single `lsnode` process, **not** Docker (docker is not installed on the box). The committed `Dockerfile` / `docker-compose.production.yml` describe an intended container model that prod does not currently use.
- **App dir:** `nodejs/` under the `scoopfeeds.com` domain root.
- **Live DB:** `news.db` (~8 GB) under the account's persisted data dir (outside the web root).

**Deploy mechanism (actual):** hPanel → **Deployments → "Settings and Deploy"** (Hostinger Git deploy; pulls the repo via the `scoop`→`scoopfeeds` 301 redirect). It advances the checkout to `origin/main` and **resets local working-tree edits to committed versions** — the local `scripts/*.sh` tweaks were discarded (confirmed not load-bearing).

**Migrations:** auto-apply on restart (`getDb` → `runMigrations`, idempotent via `schema_migrations`). 008/009/010 applied against the live DB — **new empty tables only; existing data untouched.**

**Delta deployed:** `66e1bfb → afdcfa8` = the entity stack (all flags default OFF) + docs. Verified clean / near-neutral; one new npm dep (`compromise`) is present but **not exercised until Phase 4a**.

- **GATE (met):** migrations applied cleanly; flags confirmed OFF/inert; site healthy (homepage 200, health 200).
- **ROLLBACK:** redeploy a prior commit via the same Git-deploy flow — trivial while flag-OFF.

> **Known issue (tracked).** `stderr.log` shows a Rust/tokio panic — *"OS can't spawn worker thread (os error 11)"* — a background worker hitting Hostinger's process/thread cap. **Not affecting the live site** (homepage/health return 200). **Investigate the thread ceiling before the Phase 4a backfill** (the backfill adds concurrent work and must not trip the same limit).

### Phase 4 — Ordered enablement on prod *(the actual go-live)*
> **Take a full prod DB backup before 4c** — the restore-point for the first graph-mutating step.

> **Env / flags mechanism (pinned).** Prod has **no `backend/.env`**; runtime env is injected by the hPanel Node.js app config. The loader (`backend/src/config/env.js`) **also** reads a persistent file at `$HOME/.scoopfeeds.env` (outside the repo → survives Git deploys) and sets a key **only if not already in `process.env`**. `ENABLE_SCHEDULER=true` is set in prod, so the single LiteSpeed-Node process runs the scheduler/enrich cycle. **Phase-4 flags belong in `$HOME/.scoopfeeds.env`**; they take effect on app restart. **CAUTION:** a restart briefly double-runs the app → watch the LVE ceiling; recover with hPanel stop/start if forking fails.

- **4a. Entity extraction + working-set backfill (OFF-HOST).**
  - **Constraint found:** running extraction inside the live web process is **not viable on this host.** At `ENTITY_EXTRACTION_BATCH=50` in the enrich cycle, extraction load + the brief process double-up at restart pushed the account past its **CloudLinux LVE process/memory ceiling** — the shell hit `fork: Resource temporarily unavailable` and SSH login was refused until a clean hPanel stop/start (via redeploy) reaped the pile-up. The public site stayed **200** throughout (the app serves on its event loop; only forking *new* processes failed). Same ceiling is behind the earlier tokio thread-spawn panic and the reason video rendering was offloaded to GitHub Actions.
  - **Resolution:** `ENTITY_EXTRACTION_ENABLED` stays **OFF** on prod. Extraction runs **off-host** on a copy of the prod DB (Mac, no LVE ceiling); the populated entity tables then **sync up to prod as a pure bulk-insert** (no NER, no Wikidata calls on the host).
  - **Scope:** go-live working set = **vectorized recent-3d ∪ event-members** (the Phase-1 set, ~3,209), newest-first — exactly what the matcher/breaker/IDF consume. The full historical corpus (~23.5k) is optional completeness, deferrable (~5–6 h off-host at ~865 ms/article; cache trims only the Wikidata tail, ~15–20%).
  - **Status — backfill DONE (off-host):**
    - Local copy = the gitignored copy of the snapshot. Pilot (300) + working-set (3,206) = **3,506 processed.**
    - Working-set coverage **3,209 / 3,209 = 100%**; QID resolution **78%** (pilot 75%); **46 min** wall, **~865 ms/article** warm. Prod untouched (snapshot pristine).
    - **SYNC TO PROD — DONE & VERIFIED.** Built a **2 MB sync DB** locally (3 tables; `article_entities` sans the autoincrement `id` so prod re-assigns — dedup is on the unique key `(article_id, COALESCE(qid,surface_norm))`), transferred, loaded on prod via atomic `INSERT OR IGNORE` (`BEGIN IMMEDIATE` + `busy_timeout=30000`; lock-tolerant, LVE-safe — 2 MB of finished rows, zero compute on host).
      - **Prod pre-load:** 0/0/0 (tables empty — the aborted flag-on run wrote nothing → clean first load).
      - **Prod post-load (VERIFIED):** `article_entities`=**14,283**, `surface_qid_cache`=**10,361**, `article_entity_processed`=**3,506**, `resolved_qids`=**11,048 (77%)** — exact match to the local working set. Site stayed **200** throughout.
      - **Rollback (unused):** `DELETE FROM` the 3 tables (inert until 4c).
    - `ENTITY_EXTRACTION_ENABLED` remains **OFF** on prod; tables inert behind the matcher gate (4c, OFF).
  - **GATE (met):** `article_entities` covers the working set on prod (100% of the ~3,209).

> **Host / LVE note.** The transfer hit the CloudLinux LVE ceiling a **3rd time** — SSH/SCP refused (*"connection reset / closed by remote host"*) under process/memory pressure; a redeploy did **not** clear it; it cleared only after a **Hostinger resource boost**, after which `scp` succeeded. The pattern (tokio panic → extraction fork-storm → SSH/SCP refusal) makes the **VPS migration** (Hostinger KVM: root, real process headroom, Docker for the committed stack, no LVE wall) increasingly **non-optional**. Scope it before 4b/4c add scheduler-side entity load.
>
> **Ongoing-extraction gap.** In-process extraction stays **OFF**, so **new articles ingested going forward get no entities** — matcher coverage is the synced working set, **not self-updating**. Fine for go-live validation, but "keep new stories covered" is **unsolved on this host** (in-process = the LVE wall). This is the **strongest driver for the VPS migration** — scope it before relying on the matcher long-term.
- **4b. Entity IDF — DONE & VERIFIED.**
  - **Mechanism:** the `entity_idf` recompute is **pure SQL** (windowed `COUNT` + `GROUP BY` over `article_entities ⨝ articles`, atomic table-replace) — light and host-safe, **not** the LVE risk extraction was. The scheduler recompute is **cron-only** (daily 03:30); there is **no run-at-startup**, so `entity_idf` was **pre-populated via a one-shot** so the matcher (4c) never reads an empty table.
  - **Run:** a one-shot ran the committed `runEntityIdfRecompute()` against the **live DB** (no restart). **NOTE:** the app runs on **alt-nodejs18** (better-sqlite3 `NODE_MODULE_VERSION 108`) — a standalone node one-shot must use the alt-nodejs18 node binary and set `SCOOP_PERSISTENT_DATA_DIR` explicitly (standalone procs don't inherit hPanel-injected env; the login shell has no node on `PATH`).
  - **Result (VERIFIED on prod):** `entity_idf` keys=**5,101**, `n_window`=**2,637** (windowed subset of the 3,506 synced — older/pruned rows correctly drop out), idf range **1.77–7.88** (=`ln(N)` ceiling at df=1), max `cat_span`=**14**. Common keys (Q668 India, Q794 Iran, Q30 USA) correctly **low-idf**; hubs (Q30 span 14, Q794 span 11) carry **high cat_span** for the matcher to down-weight (3b-1b). Known benign noise: unresolved site-name boilerplate ("day first show news") is high-df → low-idf → self-discounting, no action.
  - **Flag:** `ENTITY_IDF_ENABLED=true` appended to `$HOME/.scoopfeeds.env` (arms the daily 03:30 cron; takes effect on next restart). **No restart performed in 4b.** `ENTITY_EXTRACTION_ENABLED` stays `false`.
  - **GATE (met):** `entity_idf` populated, `cat_span` computed and sane (hubs high, common-entity rarity low).
- **4c. Gate + breaker.** Set `EVENT_ENTITY_MIN > 0` and `EVENT_BREAKER_ENABLED`. The matcher now uses entities; the breaker splits the existing blob. The blob dissolves on the FIRST enabled cycle (the sweep converges within-cycle, bounded by `EVENT_BREAKER_MAX_PASSES`=6); steady-state is ~2 small splits/cycle. **GATE:** blob dissolved, new matching coherent, dossier ids stable, no oscillation. *(Backup taken before this step.)*
- **4d. Display grouping.** Set `HOMEPAGE_GROUPING`. Facet cards consolidate. **GATE:** Iran → one card (+ related), distinct stories separate, lead coherent.
- **4e. Re-apply the comprehension homepage.** Re-apply `c9862ef` on the now-coherent + grouped graph (it consumes `related[]`). **GATE:** homepage renders the redesign and leads coherently.

### Phase 5 — Level-2 visual smoke + go / no-go
- Eyeball the **live** homepage: coherent lead, Iran consolidated, distinct stories separate, dossiers resolve, no blob, no thrash.
- **Decision:** relaunch or hold.

---

## Rollback posture
- Every layer is flag-gated → flip any flag **OFF** to disable it.
- The comprehension homepage re-apply is **git-revertible** (as it was once before).
- The one thing that is **not** a simple flag-flip-back is the durable graph re-clustering from 4c — covered by the **pre-4c full prod DB backup** (restore point). Keep-core preserves dossier ids through enablement, so dossier URLs survive.

---

## Deferred / off the critical path
- **Entity-disjoint guard** on the breaker/grouping confirm (for the 2 benign near-duplicate market-filler fusions): later, only-if-needed. It reaches back for the entity signal proven unreliable across three levers, so avoid unless a real user complaint surfaces.
- **`stash@{0}`** (superseded Phase 2/3b matcher edits): droppable anytime.
- **Promote the faithful replay harness** (`_phase2b_replay.mjs`) to a committed location (e.g. `backend/scripts/`): non-urgent, but it is the validation bed for Phase 1 and currently lives only as an untracked working-tree file.

---

## Reference — what "done" looks like
The incoherent over-merge blob no longer exists in the durable graph; the homepage leads with a coherent real story; the Iran-class macro-stories present as a single card with facets as `related[]`; distinct same-domain stories remain separate cards; every card resolves to a stable durable dossier; and none of it required a fragile threshold — the cluster-window arbiter carries the grouping, and the durable graph is never mutated at render.

---

## Execution log

**Phase 0 — Bank the code: DONE.** Full stack on `main`, flags OFF.

**Phase 1 — Migration validation: VALIDATED (GREEN).** On a COW of the fresh prod snapshot (1807a/16c blob — the real present-day graph), enabling the entity system dissolves the blob in place to a coherent ~324a Iran event, 100% id-stability (all live durable ids retained, dossier URLs survive), 0 collateral splits, no new blob over 3 forward cycles. The curative breaker is a viable migration mechanism — **no Phase 1b re-cluster migration needed.** (Backfill: 3,393 articles, 99.7% coverage; IDF 5,043 keys.)

**Phase 2 — Scheduler wiring + singleton-detach: DONE** (`694d12f`, flags OFF). The breaker runs to convergence as a post-promotion curative sweep over active events (`EVENT_BREAKER_ENABLED`); `detachOrphans()` un-events orphans that are both entity-disjoint and cosine-far (`EVENT_BREAKER_DETACH`). Validated on COW: blob → 278a/9c clean (46 orphans detached / 0 from the Iran core, 20/20 spot-check foreign), ids 100% stable, no re-absorb, homepage clean.

**Phase 3 — Deploy to prod (flags OFF): DONE.** Deployed `afdcfa8` via Hostinger Git deploy (`66e1bfb → afdcfa8`) onto the **LiteSpeed Node** run model (single `lsnode` process, not Docker). Migrations 008/009/010 applied on restart (new empty tables only; existing data untouched); site healthy (homepage 200, health 200); all flags OFF/inert. **Known:** a Rust/tokio thread-spawn panic (*os error 11*) from a background worker hitting the host's thread cap — env/resource, non-blocking to the live site.

**Phase 4a (off-host extraction) — backfill DONE, sync pending.** Confirmed in-process extraction is **not viable** (LVE ceiling: `fork` exhaustion at batch 50 + restart double-up; site stayed 200; recovered via hPanel stop/start). Pivoted **off-host**: working set **3,209 / 3,209 = 100%**, 78% resolved; prod untouched; `ENTITY_EXTRACTION_ENABLED=OFF` on prod. Env mechanism mapped (`$HOME/.scoopfeeds.env`, deploy-safe; `ENABLE_SCHEDULER=true`).

**Phase 4a — COMPLETE** (extraction off-host + sync to prod). **VERIFIED on prod: 14,283 / 10,361 / 3,506** (`article_entities` / `surface_qid_cache` / `article_entity_processed`), resolved **11,048**; prod **200** throughout; `ENTITY_EXTRACTION_ENABLED=OFF`. The transfer cleared an LVE block via a Hostinger resource boost (redeploy did not clear it).

**Phase 4b — COMPLETE.** `entity_idf` pre-populated + validated on prod (**5,101 keys / n_window 2,637 / idf 1.77–7.88 / cat_span ≤ 14**; hubs + rarity sane). Daily-refresh armed (`ENTITY_IDF_ENABLED=true`, cron 03:30, applies next restart). No restart; LVE untouched. App runtime = **alt-nodejs18** (pinned for server-side one-shots).

**Next:** **4c** (entity matcher gate + curative breaker) — the **first behavior-changing + graph-mutating step**. **Requires:** full DB backup first; a **restart** (LVE flashpoint — use hPanel stop/start, watch for double-up); **before/after homepage validation** (the honest gate). Recommend a **fresh session + cooperative connection**. Then **4d** (`HOMEPAGE_GROUPING`) → **4e** (re-apply homepage). **VPS scoping recommended before 4c** if doing it on this host.

**Phase 4c — PREREQUISITE FIX SHIPPED (Step 1 of 2), breaker still OFF** *(2026-07-07).* Host reality has moved on since the "Next" note above: prod is now the **Hostinger KVM 2 VPS / Docker** stack — **no LVE flashpoint**, restart = `docker compose … up -d` (safe). A fresh-snapshot re-validation before flipping 4c surfaced a **REJECT-and-iterate** finding and forced a prerequisite fix.

- **REJECT-and-iterate finding.** On an 11 GB md5-verified COW of the live graph, the naive 4c flip **would have failed**: the over-merge blob (`net-gain`, event `60063595…c3e0`) read **3,981 links but only 361 LIVE articles**, and **91.4% of ALL `event_articles` graph-wide (11,299 / 12,360) were dangling** — links to already-pruned articles. Root cause: the daily **03:00 article prune** (`pruneOldArticles`, `backend/src/models/database.js:909`; cron `scheduler.js:232`) deletes articles on a **7-day TTL**, but `event_articles` has **no `ON DELETE CASCADE`** and the prune never cleaned it, so every prune orphaned that day's links. The curative breaker only reduces **live** articles — so it correctly spun the foreign stories (Iran, Fifa, Venezuela, Microsoft) into separate coherent cards, yet the blob **still led the homepage as a ~2,326-count dangling-inflated mega-card**. (June's Phase 1 GREEN never hit this — that blob was mostly-live.)

- **The fix (`4f02028`).** Migration **011** = one-time idempotent delete of the 11,299 orphaned links; `pruneOldArticles()` now **also sweeps orphaned `event_articles` on every prune** so the graph can't re-rot. Re-validated on the *cleaned* COW at arm `EVENT_ENTITY_MIN=0.05` (breaker+detach ON): blob **361a/12c → 107a/4c** coherent lead (`✓1story`), foreign stories separate, **id-stability 100%**, and the scheduler-wired `runEventBreakerSweep` reproduces it in **3 passes**; flag-OFF path confirmed prod-neutral.

- **Step 1 — SHIPPED + VERIFIED on prod (flags OFF).** Full DB backup banked first: `~/backups/pre-4c-011-20260707-184345.db.gz` on the VPS + md5-verified copy on Mac = restore point. Deployed `4f02028` (`git pull` `1253051→4f02028` + `docker compose -f docker-compose.production.yml -f docker-compose.override.yml up -d --build`; this also pulled the previously-undeployed `97aaa39` port-harden — verified the untracked `!override` supersedes it, origin stays hidden on `127.0.0.1:8080`). Migration 011 auto-applied on recreate. **VERIFIED: dangling 11,299 → 0, total links 12,360 → 1,061, blob `net-gain` 3,981 → 361; all 4c flags still OFF; homepage 200 + cf-ray; no post-deploy errors in web/scheduler.**

- **Step 2 — DEFERRED (the actual breaker flip).** Setting `EVENT_ENTITY_MIN=0.05` + `EVENT_BREAKER_ENABLED` + `EVENT_BREAKER_DETACH` (→ `docker compose restart`) is **not done**, pending (a) a **soak across ≥1 daily 03:00 UTC prune cycle** to confirm `pruneOldArticles` keeps dangling at **0** in production (the recurrence fix has not yet been exercised by a live prune), and (b) an **unresolved product decision on article retention** — the 7-day TTL means event dossiers older than a week go hollow even after this fix (multi-week-dossier value vs. retention cost).

- **HONESTY — two open caveats.** (1) The COW **no-re-absorb** evidence was **INCONCLUSIVE**: the replay's forward re-promotion logged repeated `UNIQUE constraint failed: events.cluster_id` — **unresolved** whether that is a replay artifact (re-promoting into a non-empty graph) or a real matcher bug. The blob did not re-absorb in the replay, but this must be watched **live** at Step 2. (2) The post-breaker **107a/4c "Chip Stocks" lead** is coherent-per-clustering (`✓1story`) but **large and 4-category** — it needs a **human eyeball on the real homepage** after the flip to confirm it is one genuine story, not a residual mild over-merge.

**Retention & storyline architecture — DECIDED DIRECTION** *(2026-07-08; resolves the "unresolved retention decision" flagged in Step 2 above).* This settles the retention question the dangling-link fix exposed: 7-day article retention conflicts with the product promise.

- **Product decision.** ScoopFeeds' promise is **day-1-to-now story coverage** — the full arc of a story from its first article to the present, for both **years-long** threads (e.g. ongoing conflicts) **and** **weeks-long** ones (e.g. tournaments). A 7-day article TTL cannot serve that; the graph must carry durable history without carrying every article body forever.

- **Research convergence** (Google News, GDELT, Event Registry, and the Topic-Detection-and-Tracking literature all point the same way):
  1. **Long stories are HIERARCHIES, not one immortal event** — bounded **episode-events** chained into a durable **storyline**. The `net-gain` blob's unbounded Jun 14 → Jul 1 accretion into a single event is exactly the pathology this structure prevents.
  2. **Nobody retains full article bodies long-term.** The durable objects are **event records + article METADATA/references**, not full text (GDELT keeps coded event rows + mentions back to 1979).
  3. **Aging coverage distills** into summaries + a few representative articles rather than the full member set.

- **Planned phases (post-4c; each is its own INTAKE → … → gate run through the agentic workflow):**
  - **R1 — archive-on-prune.** Before `pruneOldArticles` deletes an **event-linked** article, snapshot its **metadata** (title / source / url / date / category / credibility) to an archive table. ~**250 MB/yr** at current volume. Fixes hollowing **going forward** — history already pruned is **unrecoverable**, so this is time-sensitive.
  - **R2 — storyline layer.** Add **temporal bounds** on events (auto-close after N days inactive — which also **structurally prevents future blobs**) plus **storylines** that chain related events via **entity-overlap + time**. This is the durable multi-year dossier object.
  - **R3 — lifecycle distillation.** Event states **active → dormant → distilled**; optional **differential retention** (event-linked articles keep full text longer than unlinked ones).

- **Sequencing:** soak (confirm `pruneOldArticles` holds dangling at 0 across a prune cycle) → **Step 2** (breaker flip) → **R1** (archive-on-prune) → **R2** (storyline layer). R3 follows R2. Note R1's urgency: every day at 7-day TTL, more event-linked metadata is lost for good.

**Step 2 — ATTEMPTED, HALTED, ROLLED BACK (2026-07-08). Exposed a pre-existing pipeline outage.** The breaker flip was executed and cleanly reverted with **zero graph mutation**; the attempt surfaced a bug far bigger than Step 2.

- **Pre-flip re-validation was GREEN** (fresh COW of the current post-prune graph, arm `ENTITY_MIN=0.05`, breaker+detach; MODE=full & sweep agreed): blob `net-gain` **93a/8c → 17a/2c**, **no longer the homepage lead** (demoted to #3; lead became a coherent Oil/US–Iran story), all top-6 cards `✓1story`, id-stability **100%**, no re-absorb over 3 cycles.
- **The flip did not take effect.** Set `EVENT_ENTITY_MIN=0.05` + `EVENT_BREAKER_ENABLED=true` + `EVENT_BREAKER_DETACH=true` in the VPS `backend/.env`, recreated web/scheduler/worker (flags confirmed live in-container). But a 15-min watch showed **`breaker_sweeps=0`** and the blob unchanged at 93. Verify caught it — the flip was *not* assumed to have worked.
- **Diagnosis:** the scheduler logged `❌ Event promoter failed {"error":"no such module: vec0"}` at each cycle. `eventPromoter` throws on the missing `vec0` module **before** it ever reaches the breaker (`scheduler.js:404`).
- **ROOT CAUSE — pre-existing migration regression, exposed not caused by the flip.** `sqlite-vec` is loaded only inside `initRealityIndexSchema()` (`realityIndex/schema.js:37`), which **only the web process runs**. `getDb()` is a per-process singleton. The VPS migration split the old single process into **separate web/scheduler containers**; the scheduler's connection never loads `vec0`, so **`eventPromoter` / `analysis refresh` / all clustering have been failing silently on prod since cutover** (~Jul 1, matching the blob's frozen `added_at`). Container check: web logs `🧮 sqlite-vec loaded (v0.1.9)` with 0 vec0 errors; scheduler logs **no** load line and **3** vec0 errors. **The site looked healthy (200) throughout while the intelligence engine was down.** **LESSON: add promoter/breaker *last-run* (and a vec0-availability check) to routine health probes — a green homepage hid a dead pipeline.**
- **Rolled back cleanly:** restored `backend/.env` from the pre-step2 backup, recreated containers; verified flags `unset/unset`, blob still **93 links/active**, dangling **0** — **no graph mutation**. The pre-step2 restore point (`~/backups/pre-step2-20260708-171449.db.gz`) is banked but unused.
- **Backlog noted (separate pre-existing issue):** a flood of `eventActorExtractor: no such column: a.summary` warnings in the scheduler log — needs its own look, not part of this fix.
- **NEXT:** a **combined package** — the scheduler `sqlite-vec` fix **plus** the 4c flags, shipped together. Shipping the vec fix with flags OFF is explicitly rejected: `EVENT_ENTITY_MIN=0` is the exact config that *built* the blob, so resuming the promoter ungated would restart the over-merge machine. One COW validation (promoter resumes on the backlog *without* forming a new blob **and** the breaker dissolves `net-gain`), one gated ship.

**Step 2 (combined vec-fix + 4c flags) — SHIPPED + VERIFIED on prod (2026-07-08). The blob is dissolved; the pipeline is alive.** After the halt above, the fix + flags shipped as one unit and held over a ~9.5 h / ~19-cycle soak.

- **The fix (`9f8a613`, merged `57cea93`):** load `sqlite-vec` in `getDb()` so every process (web/scheduler/worker) gets `vec0`, not only whoever runs `initRealityIndex()`. Idempotent (double-load verified harmless) + failure-logged. **COW-validated as a package** (armed = ship config): promoter resumes and clusters the backlog *without* a new blob (largest 25a/5c), breaker dissolves `net-gain` 93a/8c → 17a/2c, id-stability 100%, no re-absorb; the ungated contrast (`ENTITY_MIN=0`) proved the gate is load-bearing (28–31 merges/cycle, 7% id-stability) — hence flags ship *with* the fix.
- **Deploy:** fresh backup (`~/backups/pre-vecfix-20260708-200533.db.gz`, md5-verified) → merge to `main` + push → VPS `git pull` (`4f02028→57cea93`) → set the 3 flags in `backend/.env` → `docker compose … up -d --build` (`--build` required — code changed). **Hiccup (honest):** the first flag-set used a mis-escaped nested heredoc that wrote literal `$kv` lines into `.env`; `docker compose` failed to parse the env_file and **aborted before recreating** — so prod was unchanged. Restored `.env` from the auto-backup, set flags via a clean stdin script, re-ran `--build`. Lesson: set `.env` with explicit literal lines, never a heredoc-nested loop; verify `.env` parses before recreate.
- **VERIFIED on prod:** scheduler now logs `🧮 sqlite-vec loaded (v0.1.9)`, **vec0 errors 3 → 0**. Breaker fired on the first `:13` cron: `split=3, created=19, detached=6`; **blob `net-gain` 93a/active → 17a/dormant** (→ **1/dormant** as its articles aged out over the day). Over ~19 subsequent cycles: largest event stable at **25a/5c** (no new blob), **no re-absorb**, **no fatal errors**, homepage top cards coherent/distinct, site **200** throughout, dangling bounded (5–6, self-healing at the 03:00 prune).
- **OPEN CAVEAT → next investigation (the promotion issue is now confirmed, not just "inconclusive"):** the promoter runs cleanly but reports **`promoted=0, matched=0` every cycle** (`eligible=300`) after the initial `promoted=1`. The pipeline is **alive but not creating new events** — the `UNIQUE constraint failed: events.cluster_id` warnings are real and persistent. Blob-dissolution/homepage are unaffected, but **"keep new stories covered" is not yet restored.** This is a *separate* bug from the vec fix (it appeared in the COW too) and is the clear next item.
- **Rollback (banked, unused):** `git revert` the vec merge + flags OFF + `--build` re-freezes to the pre-ship state; or restore the pre-vecfix snapshot. NOT "flags off alone" (that leaves the promoter ungated). **Still open, separate:** the `eventActorExtractor: no such column: a.summary` flood.

**Singleton-dedup-trap fix + ongoing entity extraction — SHIPPED + VERIFIED (2026-07-09/10).** Chasing the `promoted=0` caveat above surfaced a **second silent green-logs/dead-pipeline outage**, bigger than the promotion bug itself.

- **FOUND:** `fetched_at` newest = Jul 8 09:21 UTC — **zero articles ingested in 24+ h**; site serving stale content. All three BullMQ queues (ingestion/video/enrichment) had executed **exactly once — Jul 3 06:55 UTC** — then never again, while the scheduler logged `📥 Enqueued …` every 30 min. **Root cause: the singleton-jobId dedup trap.** `enqueueSingletonJob` reuses one jobId per cycle job; `removeOnComplete:{count:100}` never prunes a queue whose *only* job is that singleton; BullMQ refuses to re-add a jobId that still exists (even completed) — `queue.add()` returns the stale finished job and nothing runs. Jul 3→8 article flow was some in-process fallback that stopped ~Jul 8 09:21 `[unverified which]`.
- **FIX (`108acb3`):** in `enqueueSingletonJob` (`jobs/queues.js`), before `add()`: fetch the existing job by jobId; if completed/failed, `remove()` it. Waiting/active/delayed left alone (overlap-prevention is the singleton's purpose). Self-healed the three stale prod records on the first cron tick — no redis surgery.
- **VERIFIED on prod (watcher over 21:00–21:35 UTC):** ingestion resumed at 21:00:02 after ~36 h dead — **166 fetch_completes / 40 min, 1,683 articles fetched in the first hour**; singleton records re-created fresh **every cycle** (not once-then-dead); zero unexpected errors.
- **Ongoing entity extraction — LIVE (the runbook's "ongoing-extraction gap" is closed).** With enrich cycles actually running, the armed pilot executed: `ENTITY_EXTRACTION_ENABLED=true`, `BATCH=25` → 4 clean batches (entities 68–94/batch, **~80% QID-resolved**, cache warming 20→53 hits, 60–71 Wikidata calls/batch, no error storm), `article_entity_processed` 3,506→3,606. **Step B applied:** `BATCH=100` (worker-only recreate) — backlog (~7.3k + influx) clears in <1 day at 9,600/day capacity. Extraction runs **in the worker container**, isolated from serving and the promoter; old LVE fork-storm concern does not apply (no forking; in-process JS + cached HTTP).
- **EXPECTED NEXT (watch, then verify):** as entity coverage reaches the active window, the `ENTITY_MIN=0.05` matcher gains signal → `matched>0`, then `promoted>0`, UNIQUE-collision warnings subside, graph grows. **The `promoted=0` caveat above is expected to resolve via this chain — verify against live promoter stats rather than assuming.**
- **LESSON (now twice-earned):** green scheduler logs proved nothing — "Enqueued" ≠ "executed." **Add a pipeline-liveness probe** (last-executed timestamps for ingest/enrich/promoter + vec0 availability) to the health endpoint / soak checks. Tracked as a follow-up alongside R1 (archive-on-prune), the retention/storyline phases, and the `a.summary` flood.

**Event-creation restoration (identity release + embedding fix + promotion guards) — SHIPPED + VERIFIED (2026-07-11, `83b56b8`).** Chasing the confirmed `promoted=0` caveat found TWO more silent regressions from the container split, fixed together with the identity bug in one gated package.

- **Diagnosis chain (each step verified on prod/COW):** (1) merged tombstones kept their `cluster_id` (a title-hash) while excluded from match candidates → recurring stories collided on `UNIQUE(events.cluster_id)` forever (~290/cycle; all sampled colliders = merged, 0 live members). (2) The eligible-300 were mostly **stale market-linked clusters** — releasing identities alone would have junk-flooded the graph. (3) The real starvation: `vecAvailable` is set only by `initRealityIndex()` (web-only), so the **worker ingested every article with `embedDocument()` silently no-oping** — 2% of <3d articles embedded vs 78% at 3–7d → no fresh clusters → nothing to promote. Same per-process-flag class as the vec0 bug.
- **The package:** (A) `markMerged` NULLs `cluster_id`; (B) migration 012 released **669** tombstone claims; (C) promoter insert path reclaims from dead holders; (D) creation-freshness guard `EVENT_PROMOTE_MAX_CLUSTER_AGE_MS` (48h) — stale clusters can match but not spawn; (E) `markVecAvailable()` from `getDb()` so embedding works in every process. COW validation on prod-condition `story_clusters` caught a **churn bug** in the naive reclaim (an entity-less cluster re-promoted a duplicate every cycle) → fixed with **adopt-don’t-steal** (a living holder sharing articles with the cluster IS its event; run1==run2, 0 duplicates).
- **VERIFIED on prod (15:10–15:51 UTC):** collisions **290/cycle → 0**; `skippedStale≈289`; embeddings growing again (17,168→17,212 in 40 min); **15:18 UTC `promoted=1` — first organically created event since Jul 1**; matched 11→12 (new event matches next cycle, no churn); zero errors; site 200. Restore point `pre-clusterid-20260711-142339.db.gz` banked, unused.
- **THE `promoted=0` CAVEAT IS CLOSED.** Ingest → embed → cluster → gate-checked match/promote is alive end-to-end. Watch coverage: embedding rate vs ~3k/day influx over the next day [unverified at ship time].
- **Backlog (unchanged):** pipeline-liveness probe (three silent outages now argue hard for it) · R1 archive-on-prune · 4d/4e · `eventActorExtractor a.summary` flood.

**Pipeline-liveness probe — SHIPPED + VERIFIED (2026-07-11, `c16436e`).** The three-outage lesson made operational. `/api/health` now carries a `pipelines` block derived purely from DB MAX-timestamps (ingest/embed/extraction/clusters/event_activity/event_creation, each `{newest, age_min, stale}` with cadence-sized thresholds) + `vec_available` + dangling-links count + a single alertable `pipelines.ok`. Logs and in-process counters can lie across the container split; the database can’t. Validated on a stale COW (correctly flagged dead stages), then live: **all stages fresh, ok:true, event_creation age 16m** (organic creation flowing). Routine soak check is now just `curl -s https://scoopfeeds.com/api/health | jq .pipelines`.

**R1 archive-on-prune — SHIPPED (2026-07-11, `46acab7`); first-prune verification pending.** Migration 013 adds `event_article_archive` (title / source / url / category / published_at / credibility per the decided direction; PK event+article), and `pruneOldArticles()` now snapshots event-linked articles' metadata BEFORE the delete + orphan sweep — dossier history stops being erased daily. COW-validated exactly (archive == expected prunable set 18/18, dangling 0, idempotent). Deployed; 013 applied (empty table); **verify after the next 03:00 UTC prune: archive rows > 0 and `pipelines.ok` stays true.** Write-only until R2 (storyline layer) consumes it. Remaining backlog: 4d/4e (give the growing graph ~a day first), `eventActorExtractor a.summary` flood.

**2026-07-09 — embedding quota finding + interim fix (paid Gemini).** Overnight soak GREEN incl. **R1 first-prune proof** (03:00 prune ran, archive populated). The new pipeline probe caught a daily **Gemini free-tier embedding stall** — 429s after ~400 requests/day → only ~24% of daily articles embedded, so clustering saw a quarter of the news. **DrJ flipped the Gemini key to PAID tier** (billing change in the Google console, budget alert set) — interim fix; the **permanent provider decision is logged as a Phase-C gated item in the decisions log (Decision 32)**, pre-scoped: multilingual, quota-free/owned at scale, bundled with matcher re-calibration, sequenced with R2. **VERIFY over next 24–48h:** 429s stop, embed coverage climbs toward ~100%, event promotion rate rises accordingly (graph should visibly thicken — more/fuller events). Related backlog unchanged: `eventActorExtractor "no such column: a.summary"` warning flood still open; Bluesky/Threads passive checks still pending.

**4d + 4e — SHIPPED (2026-07-12): grouped homepage live; Phase 5 visual go/no-go handed to DrJ.** Warm-up first: fixed two dead dossier features (`e90f8a6`) — eventActorExtractor and eventTimelineBuilder queried non-existent `a.summary`/`a.credibility_score` (~10k silent failures/day since forever); aliased to the real `description`/`credibility` columns. Then **4d**: `HOMEPAGE_GROUPING=true` on prod (web-only recreate) after COW validation on the current graph — duplicate Le Pen events consolidated to one card + `related[]`, 20→18 cards, distinct stories separate. Live API verified: `grouped:true`, 20 events → 11 cards, 5 with `related[]`. **Honest quality note:** strong pairs group well; the low-prominence tail shows cosmetic mis-groups (e.g. a regional single-source card pulling an unrelated story) — per design ("a mis-group is one cosmetic card, never a blob"); `HOMEPAGE_GROUP_TAU` (0.86) is the conservatism knob if the eyeball says too aggressive. Then **4e**: re-applied the comprehension homepage (`2c7390c`, byte-identical to `c9862ef` by patch-id; cherry-picked clean) — originally reverted when the graph was broken, now rendering a healthy, growing, deduplicated graph. Verified live: homepage 200, redesign rendering (Top-stories band with outlet/article consolidation badges, Developing rail, Most-sourced), `pipelines.ok:true`. **NEXT = Phase 5 (human):** DrJ eyeballs the live homepage — coherent lead, macro-stories consolidated with facets, distinct stories separate, dossiers resolve, no blob, no thrash → relaunch or hold. Rollbacks: 4d = flag off; 4e = git revert (proven clean once before).

**R2a — SHIPPED + VERIFIED (2026-07-12, `2604652`): temporal event bounds + durable entity signatures.** Migration 014 adds `event_entity_signature`; the promoter now persists each matched/created event's entity core (top-K keys + idf) while members are alive — the identity an event keeps after the 7-day prune. `EVENT_CLOSE_ENABLED=true` live: dormant events inactive >21d (`EVENT_CLOSE_AFTER_MS`) become `closed` — excluded from every consumer by existing status filters, dossiers still render (verified live on a closed slug). First cycle: **closed 21,190** (one-time backlog, as COW predicted), 0 false closes on COW, **33 signatures** and counting; steady-state stats healthy (`promoted=10, matched=26`). Unbounded accretion is now structurally impossible. **R2b (storyline chaining) is the follow-on gated item** — needs a few days of signature accumulation first, threshold calibration on COW before ship. **Still pending on DrJ:** the Gemini key/project billing linkage (probe shows `FreeTier 1,000/day` violation — embed coverage capped ~40-60% until fixed).

**R2b — SHIPPED, HALTED, FIXED, RE-SHIPPED + VERIFIED (2026-07-12; `21dd486` → `05aebbb`): storyline chaining live.** Migration 015 (storylines + storyline_events); candidate-load signature refresh back-fills durable identity for all live-member events; chaining on event creation behind `STORYLINE_ENABLED`. **First enable failed fast and honestly:** first cycle chained 10/10 new events, incl. a police seizure and a singer obituary onto the same prior at overlap 1.0 — the score divided by the NEW event's keys only, so any small key-set inside one big signature scored perfectly (hub magnetism). Flag off within minutes; 14 junk rows deleted; **never user-visible** (R2b is data-only by design — this incident is why). **Fix (`87f861b`):** weighted Jaccard (symmetric), ≥2-shared-keys + ≥10 idf-mass evidence floor, and idf-fallback discipline (live idf → prior signature's STORED idf → 1; never the max-rarity fallback, which poisons cross-month comparisons as keys age out of the rolling window). COW recalibration: distinct-pair max overlap 1.000 → 0.000 (156 pairs); recurrence still 1.0; the live-failure shape scores ~0.1 < 0.25 floor. **Re-shipped, first cycle verified: chained 4/9,** 3 textbook episode recurrences (Qatar Emir death → retrospective; Graham tributes pair; Argentina pair) + 1 debatable person-level chain (Graham death → an evolved-title event that was originally Graham-centric) — noted as the person-vs-story tuning observation. Every chain logs 🧵 with overlap for ongoing audit. Known v1 limits (accepted): parallel storylines for one macro-story (no storyline merging yet); read surfaces (dossier "story so far") are the next item once chain quality holds for a few days.

**Decision 32 interim fix — VERIFIED IN EFFECT (2026-07-12).** DrJ completed the key↔project billing linkage; the quota probe now returns 200 + embedding (was `FreeTier 1,000/day` violation). One-shot backfill embedded the 429-era gap: **6,090/6,092** unembedded articles from the last 3 days (2 fails, empty-text). **Embed coverage: 100% across the 3-day window; 0 429s.** Full clustering coverage restored — the daily-afternoon embed stall pattern should not recur (watch one afternoon to confirm). Last open human item: Phase 5 homepage eyeball.

**Sprint 1 (post-audit remediation) — SHIPPED + PROD-VERIFIED (2026-07-14, merges `e46cd3d..8694d2a`).** The 2026-07-13 audit's outage-class findings, fixed as five small gated ships (G3 ×5 + G4, DrJ-approved), each verified in a browser on live scoopfeeds.com per the new product-level evidence rule (mechanism-level verification alone caused the NO-GO).

- **S1c `/api/events` 524 (audit P0-2):** NOT a query problem — the endpoint is an SSE stream that global `compression()` buffered forever, so Cloudflare saw 0 origin bytes → 524. Fix: exempt `text/event-stream` from compression, flush per write, drop scheduler internals from heartbeats, remove the consumer-less frontend EventSource. LIVE: `event: connected` arrives instantly through Cloudflare.
- **S1a runaway anomalies poll (audit P0-1):** root cause `sinceMs: Date.now()` inside the React Query key → new key every render → fetch-render loop at ~420ms (~2.4 req/s against the 240/15min IP budget). Fix: quantize sinceMs to 10-min buckets, exponential failure backoff (cap 30 min), no background-tab polling; the two ops-page setInterval pollers gained hidden-pause + failure backoff. LIVE SOAK: 10 minutes fronted on /events → **11 API requests total, ONE anomalies fetch, ZERO 429s** (the same window produced ~1,500 anomalies requests pre-fix).
- **S1b honest degraded states (audit P0-1 UX):** all `/api/ri/events*` hooks moved onto the 429-intercepting `createApi`; **positive-evidence rendering**: "Event not found" only on a definitive 404; empty-feed copy only on a successful empty response; every other data-less state — 5xx, network, swallowed 429, **and React Query's offline-paused retry (no data AND no error; caught during local verification, hits flaky mobile connections constantly)** — renders "Temporarily busy — retrying…" with real retry. The "run a clustering pass" debug string is gone. LIVE: garbage slug → genuine not-found; real dossier renders; offline-emulated uncached dossier → temporarily-busy → loads on reconnect. (Environment note: in-place auto-resume rides the browser's genuine `online` event / React Query networkMode resume — not exercisable with synthetic events in the verify harness; reconnect-then-load verified.)
- **S1d rails dedup (audit P1-12):** Developing excludes the Top band; Most-sourced excludes both; rails shrink honestly rather than pad. LIVE: **7 band + 3 Developing + 1 Most-sourced = 11 slots, 11 distinct stories, 0 repeats** (audit day: ~7 stories across ~19 slots; the raw ≥12 target is graph-population-dependent — every slot is now unique, which is the metric's intent).
- **S1e trivia batch (audit P1-10 + P2/P3):** `COPY.brandTagline` un-invoked-function leak fixed at **5** call sites (audit saw 1; Finance ×2 + SyntheticMarkets ×2 rendered nothing); homepage h1; honest meta/OG descriptions; `/api/health` trimmed to status/counts/pipelines (scheduler blob, DB path, memory dropped — verified no consumer); robots.txt `Disallow: /search?` per Decision 30 (in `seo.js` — the static file is shadowed in prod; the bare /robots.txt URL is Cloudflare-edge-cached max-age 14400 and was verified correct at origin via cache-busted URL — self-expires ≤4h); onboarding selected-tile contrast + a11y. LIVE: subtitle proper English; `<h1>Top stories</h1>` in DOM; new meta in raw HTML; health trimmed (uptime 83s post-recreate); selected "Pakistan" tile label rgb(30,45,110) on rgb(239,246,255) ≈ 12:1 contrast (was ≈1:1 invisible), `aria-label` + `aria-pressed` on all 8 option tiles.
- **Deploy:** DrJ per C8 (VPS pull `cfad23c→8694d2a`, compose build + force-recreate web/worker/scheduler; untracked override preserved the localhost:8080 binding). No migrations; no restore point required (nothing touched events/event_articles/timelines).
- **OBSERVED DURING VERIFY (pre-existing, NOT Sprint-1 regressions — feeds Sprint 2 GROUND):** (1) `pipelines.embed` stale **804 min** at verify time — the daily-afternoon embed-stall pattern re-appearing despite the 07-12 D32 paid-tier verification; check quota probe + 429s on the VPS. (2) Cross-event **card-summary contamination** (the Kannada obituary card carries a "Zelensky Announces Leadership Shuffle" summary — on the LEAD card no less). (3) Article-count inflation continuing (Kannada 1-outlet event 42→**153** articles; Iran 47→98) — 2c duplication is live, not just scar tissue. (4) The single-source Kannada event is now the **#1 Top Story** (2e escalated). (5) **Eight raw NOAA machine-slug events top the default active-events list**, one rendering as a "Weather / LIVE" dossier of bulletin text (Sprint 3a evidence).

**ADDENDUM — registered backlog (DrJ, 2026-07-14):**
- **A1** Card sub-links (related[]/facet links on homepage cards): gated behind Sprint 2a/2b outcomes — no facet UI on top of a graph whose merge/churn behavior is still being corrected.
- **A2** Dossier restructure: Coverage/Angles presentation (replacing the flat Perspectives/Articles split) — design item, post-Sprint-2.
- **A3** Modules-earn-their-render rule (no empty Trackers/Reality-Index placeholders on dossiers where they carry no data) — Sprint 3 scope.
- **A4** Post-re-audit backlog: performance pass (Lighthouse 50→90: payload, code-split, render-blocking, edge s-maxage/SWR), SSR + per-event meta/NewsArticle JSON-LD, homepage 9-fetch consolidation vs the 240/15min budget, useAuth per-mount fan-out, global focus-visible ring — scheduled workstream AFTER Sprint 3; gates the re-audit exit-criteria table, not the integrity work.

**BLUESKY — RESOLVED (2026-07-14):** the dark-profile mystery was a stale handle — account upgraded to `@scoopfeeds.com` (retiring `nmc192.bsky.social`); handle + fresh app password set on the VPS, probe 200. Doc profile links updated (vps_migration_v1.md); lesson recorded in `docs/ops/social_credentials.md` (platform identity changes are publisher config changes). The runbook's "Bluesky passive checks pending" item is CLOSED.

**NEXT:** Sprint 2 INTAKE (2a-2e, diagnosis-gated: GROUND with evidence → 🛑 fix proposals → build). HARD STOP after this docs commit.

**LLM cost incident — CONTAINED + VERIFIED + CLOSED (2026-07-15/16; gate a = `cc78c48` + `bad4bb9` + `a98a276`).** ~$25.77 burned in ~1 day on the paid Gemini key ($23.33 = OUTPUT-token SKU); credit exhaustion also stalled embeddings (2a blind window: 2026-07-14T07:35Z → re-credit 07-16). Grounded before fixing (3-scout call-site inventory + prod env/DB answers + SKU split), per the workflow.

- **Mechanism (proven live by the rider-1 pre-test):** `gemini-flash-latest` silently resolved to a THINKING model — reasoning bills as output; thinking consumed capped calls' budgets → empty/truncated JSON → nothing persisted → hourly re-pay forever. Incident config on a real extractor prompt: **thoughtsTokenCount 489/512, candidatesTokenCount 8, finishReason MAX_TOKENS, parse FAILED**. Gate-(a) config: **STOP, 0 thinking tokens, 1447-char clean JSON, 416 output tokens**. Top burner: the 07-12-repaired actor extractor (re-paying every active event hourly, `event_actors` stayed 0 the whole time); second tier: refreshAnalysis re-briefing unchanged clusters every 30 min with NO output cap; with only GEMINI_API_KEY set, every llmQueue task also routed to the paid key (the D32 drift pattern, generation side).
- **Rails shipped:** pinned model + `thinkingBudget:0` on all three Gemini paths (graceful degrade if rejected); output caps on the two uncapped direct sites; actor attempts ledger (migration 016: retry cap 3, 6h/24h spacing, content hash) + articles-first slot ordering (article-less NOAA/churn junk mints ids faster than any budget — it had eaten all 25 slots on free ledgering, zero actor LLM calls for two days); refreshAnalysis idempotency (sorted-article-id set vs stored `article_ids`); global `LLM_DAILY_CALL_CAP` (2000/day, DB-persisted, per-task tags); deep-dive endpoint cache-only for anonymous; 404 model-gone handling (loud ERROR once per model + deterministic fallback — a pinned model can be deprecated out from under you).
- **Pin saga (both riders earned their keep):** signed-off gemini-2.5-flash was DEAD for this key (404 "no longer available to new users"). Re-pinned from the key's real list via `--list-models`: **gemini-3.1-flash-lite** (**[pending]** rates NOT yet recorded — the VPS .env comment still carries placeholders; DrJ to fill. The prior wording here asserted compliance without verification; corrected 2026-07-16). Pre-test CALL B green on prod.
- **VERIFIED LIVE + CLOSED (2026-07-16):** 🎭 `candidates 25, llm_calls 17, skipped_by_ledger 8, actors_upserted 93` per cycle; `llm_daily_calls` carries an `actors` row (93); **event_actors 0 → 476 and climbing — audit P1-#6 (dead actors) is DEAD**; anonymous deep-dive returns cache-only shell (verified from outside); other tasks billing normally under per-task counters.
- **LESSONS:** (1) never a floating "-latest" alias; (2) a pin can be deprecated out from under you — 404 = loud + fallback + re-pin from the list; (3) pins come from `--list-models`, never docs — and the list can contain ids that still 404: the pre-test CALL is the only proof; (4) `docker compose exec` runs the BAKED image (use `docker cp` for one-off scripts, or rebuild); (5) Caddy log directive needed restart, not reload; (6) `caddy validate` as root chowns log files; (7) cost visibility is a rail, not a report — usageMetadata was discarded and nothing counted calls, so the burn was only found at credit exhaustion. **QUEUED (gated):** (b) explicit provider-env contract + the two hardcoded Gemini sites through llmQueue + Cerebras/Groq tiering; (c) usageMetadata capture + token counters + budget alarm; permanent embedding project (D32 pulled forward: ollama bge-m3 + matcher recalibration, <$5/month target).

**Sprint 2c — SHIPPED + PROD-VERIFIED (2026-07-16, `5a59140`/migration 017).** `event_timeline` never had a unique constraint — the builder's INSERT OR IGNORE had no conflict target; every hourly run re-inserted every row for every active event (70% of the table was duplicates; audit P1-#5). Dedup keeping MIN(id) + partial UNIQUE index (ref_id IS NOT NULL). COW-validated exactly; prod: **185,487 → 55,661 rows**, restore point banked pre-017. Live: Iran dossier timeline renders each entry once (148 rows/147 distinct via API, screenshot in session), badge deflated, `pipelines.ok:true`.

**Sprint 2 Wave 1 — SHIPPED + LIVE-VERIFIED (2026-07-16, `5bb68fd`/`c0b4d6b`).** No-shells (1-to-1 losers absorb instead of spawning duplicate events), summary rides the title's cluster-size protection (the Kannada-wearing-Zelensky stamp path is dead), 🧭 promoter decision log, extractor articles-first ordering, ♻️ brief-reuse visibility. COW: control +63 events/2 cycles vs patched +34 (−46%), zero lost-1-to-1 creates. LIVE: 🧭 flowing and showing Wave 2's exact portrait (every create = gate-rejected at cos 0.98 + shared_article:true + ent 0.02-0.05 — diluted-core rejection); families minting at the reduced rate the honest criterion predicted. **♻️ finding:** `reused: 0` every cycle (analysis-brief 498/day) — hash verified stable-by-construction (sorted article-id sets; lookup key derives from the anchor); zero-reuse is the sliding 24h window + hot-cluster influx + treadmill churn. **Parked as a Wave-2 success criterion: ♻️ reused > 0 within a day of the flag flip** (expect nonzero-not-dominant — hot clusters legitimately re-brief).

**NEXT:** Wave 2 (`feat/unified-affinity`, `e66f50e`) at its ship gate — one story-affinity judgment (idf-weighted containment, hub filter cat_span≤3, AFFINE≥0.23/FOREIGN<0.19 with AMBIGUOUS hysteresis) for promoter/merge/breaker behind `EVENT_UNIFIED_AFFINITY`; COW: creates −96%, Iran family FROZEN, Kannada attractor shrinking with no re-adoption, 385 porous merges held. Wave 3 (husk cleanup + blob dissolution + summary repair) gated on 🧭 showing families frozen live.

**Sprint 2 Wave 2 — SHIPPED (2026-07-16, `e66f50e`; dark behind `EVENT_UNIFIED_AFFINITY`).** One story-affinity judgment (idf-weighted containment over TOPK hub-filtered entity sets, cat_span≤3) with ordered verdict bands + hysteresis (AFFINE ≥ 0.23, FOREIGN < 0.19, AMBIGUOUS holds) now governs promoter attach, merge, and breaker split — replacing three judges measuring three different quantities under two hub regimes (incl. the same env var with divergent defaults, promoter 999 vs breaker 5). Coherence contract: degenerate-core events can't attract (newborns ≤8 members/<24h exempt per R1). Breaker apply is surgical (kept links keep added_at — the old delete-all-relink was the provenance-destroying churn signature). COW R3 evidence: creates −96%, Iran family FROZEN 174→174 over 4 cycles, Kannada attractor 153→117 with zero re-adoption, 385 porous merges held, 36 macro sub-threads kept whole. **Known-accepted boundary case (DrJ adjudication):** zelensky↔wheat scores 0.280 = AFFINE — inside the SAME range (min-SAME 0.248), no threshold separates it without rejecting genuine hard continuations; matches current prod behavior; 🧭 makes this error class countable; the real fix is signal-level (Wave 3+). Post-flip watch: 🧭 create-causes → genuine no-candidate (INCOHERENT-refusals persist until Wave 3 cleans blobs — expected), families frozen, held sane, Kannada stopped, ♻️ reused > 0 within a day (nonzero-not-dominant), no fight signature (breaker-split id re-appearing in promoter-merge/absorb next cycle), site 200 + pipelines.ok. Rollback = flag off + recreate.

**Wave 2 — FLIPPED LIVE + FIRST-CYCLE VERIFIED (2026-07-16 18:13Z; treadmill DEAD).** First ~53 min / 2 cycles: 🧭 poison signature GONE (zero `gate-rejected + shared_article + cos~0.98` lines); breaker converging 16→1→0 splits with banded keep decisions; counters (17:53→18:46): iran 346→347 (one transition, not per-cycle minting), fifa 111→114 (live World Cup — plausibly real sub-stories, on watch), **kannada links 1747→1747 FLAT — the garbage-attractor stopped accreting**. Promoter: promoted 4 / matched 113 / absorbed 165 / held 2 / merged 62. Site 200, pipelines.ok true. Multi-day freeze watch running; Wave 3 (husk cleanup + blob dissolution + summary repair) opens when families hold flat.

- **Live finding (DrJ watch): merge-gate small-side porosity — investigated on the COW, within-spec, fix proposed.** A vietnam-speedboat event merged into an Indian-govt survivor at ent 0.482. COW reproduction of the class (`vietnam↔lok-adalat`): containment 0.285 vs Jaccard 0.062 — the driver is the CONTAINMENT denominator (min-side mass), which makes SMALL events (7 hub-filtered keys) cheap to absorb when they share 2-3 span-1/2 regional entities (Q1361 idf 6.09, Q1445 idf 4.66). NOT a cat_span problem — the driving keys are legitimately non-hub; tightening the hub filter wouldn't catch them. Switching MERGE to symmetric Jaccard fails calibration: genuine same-story consolidation merges (iran↔iea 0.173, fifa↔tuchel 0.174) fall below T_MATCH and duplicate families would stop consolidating. **Proposed Wave-2.1 (own micro-gate):** merge-specific evidence floor — min-side entity-set size ≥ 10 AND intersection ≥ 3 keys (env-tunable) — which on current labels blocks vietnam↔lok-adalat (min-side 7) while passing iran/fifa (40/40); calibrate the floor from a day of live 🧭 merge/merge-hold lines before shipping. Bounded meanwhile: the class needs cos ≥ 0.86 + shared regional keys + a small event, and every instance is 🧭-countable.
